angular.module("ClcWorld")
    .controller("carEditController",
    ["$scope", "masterTablesService", "carService", "carId", "$uibModalInstance",
        function ($scope, masterTablesService, carService, carId, $uibModalInstance) {
            var vm = this;
        }
    ]);